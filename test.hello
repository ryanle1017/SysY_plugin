// 测试文件：包含数组大小溢出问题

int main() {
    int b = 10;
    int result;

    // 数组定义和初始化
    int numbers[6] = {1, 2, 3, 4, 5, 6}; // 数组大小溢出
    int matrix[2][2] = {{1, 2}, {3, 4}};

    result = numbers[0] + numbers[1];
    return 0;
} 