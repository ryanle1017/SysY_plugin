// SysY语言阶乘计算示例程序
// 此程序演示了递归函数的使用和参数传递

// 计算阶乘的递归函数
int factorial(int n) {
    // 基本情况：0或1的阶乘为1
    if (n == 0 || n == 1) {
        return 1;
    }
    
    // 递归情况：n! = n * (n-1)!
    return n * factorial(n - 1);
}

// 主函数
int main() {
    int number = 5; // 要计算阶乘的数字
    int result;     // 存储结果
    
    // 计算阶乘
    result = factorial(number);
    
    // 在这里可以添加打印语句(如果实现了I/O)
    // printf("阶乘 %d! = %d\n", number, result);
    
    return 0;
} 